#!/bin/bash
mysql --user=root --password="$DB_ROOT_PASSWORD" -e "GRANT SELECT, INSERT, UPDATE, DELETE, INDEX, ALTER, CREATE, LOCK TABLES, CREATE TEMPORARY TABLES, DROP, REFERENCES ON bugs.* TO bugs@localhost IDENTIFIED BY '$DB_USER_PASSWORD';"
